# This file is for non-sensitive default configuration.
# Sensitive data like API tokens should not be stored here.
# For this project, sensitive data is stored in the Windows Credential Manager via the `keyring` library.
